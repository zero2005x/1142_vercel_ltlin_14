// tweakcn Mode Toggle
import { useTheme } from 'next-themes'

const ModeToggle = () => {
    const { theme, setTheme } = useTheme()

    return (
        <div>
            {/* Mode toggle content */}
        </div>
    )
}

export default ModeToggle
    
    